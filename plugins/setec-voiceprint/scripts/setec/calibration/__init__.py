"""setec.calibration — implementations behind scripts/calibration/*.py
per-file compatibility launchers. Empty until P4; see
specs/svp-packaging-conversion.md P4 (nested capability paths receive
per-file launchers, never a directory alias)."""

from __future__ import annotations

entries:
- id: shard_runner
  script_path: plugins/setec-voiceprint/scripts/calibration/shard_runner.py
  surface: calibration
  status: todo
  todo_reason: "Registry contract remains incomplete: this auto-seeded fragment still has unresolved TODO sentinels in family, use_when, do_not_use_when, inputs.target, outputs.artifacts, compute.cost_note, and registers; code/test presence alone cannot justify discovery until those operator fields are curated."
  handoff: none
  # consumers: also surfaced by skill: validation.
  consumers:
  - setup_launchd
  - task_surfaces
  family: TODO
  purpose: sharded calibration orchestrator.
  use_when:
  - TODO
  do_not_use_when:
  - TODO
  inputs:
    target: TODO
  outputs:
    schema_version: '1.0'
    artifacts:
    - TODO
  compute:
    tier: core
    cost_note: TODO
    length_floor_words: null
  registers:
  - TODO
  dependencies:
    python: []
  examples: []
  references: []
  _seeded_at: '2026-05-28'

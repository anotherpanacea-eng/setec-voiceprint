entries:
- id: evidentiary_conditions_gate
  script_path: plugins/setec-voiceprint/scripts/evidentiary_conditions_gate.py
  surface: validation
  status: todo
  todo_reason: "Registry contract remains incomplete: this auto-seeded fragment still has unresolved TODO sentinels in family, use_when, do_not_use_when, inputs.target, outputs.artifacts, compute.cost_note, and registers; code/test presence alone cannot justify discovery until those operator fields are curated."
  handoff: none
  # consumers: the SHIPPED mimicry_cosplay_audit claim_license names "the
  # evidentiary-conditions gate" as a required pairing in does_not_license. Pinned by
  # test_contract_fixtures.py::test_mimicry_fixture_pairing_is_registered.
  consumers:
  - mimicry_cosplay_audit
  family: TODO
  purpose: front-door evidentiary posture.
  use_when:
  - TODO
  do_not_use_when:
  - TODO
  inputs:
    target: TODO
  outputs:
    schema_version: '1.0'
    artifacts:
    - TODO
  compute:
    tier: core
    cost_note: TODO
    length_floor_words: null
  registers:
  - TODO
  dependencies:
    python: []
  examples: []
  references: []
  _seeded_at: '2026-05-28'

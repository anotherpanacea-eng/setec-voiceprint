### Added

**ROADMAP "no-verdict-about-the-person" anti-goals + `specs/21-attribution-refusal-lab.md` (docs only).** Names the second anti-goal cluster the list was silent on — **no open-set author de-anonymization**, **no demographic / author profiling**, **no sock-puppet / cross-document identity-linkage verdict** — as the sibling of the existing *no-verdict-about-the-text* refusals, with the reasoning (base-rate / open-set error model; ESL-as-confound-not-label; linkage as the front end of unmasking). Adds the constructive carve-out as `specs/21` — a **research brief / anti-goal note** (deliberately NOT a dispatchable spec): a **refusal-curve laboratory** that studies these three only to demonstrate where the signal is bullshit and plot an untooled LLM's confident attribution against that curve — strength-of-evidence only (LR / posterior odds / proximity), never a name or label; consented/held-out references only (no live open-set mode, ever); a testable **E3 non-leak/redaction rule** (raw person-identifying outputs stay private; only aggregate error/confidence categories are published; a redaction scan gates publication); and a deliberately *strong* foil (the one place ship-weak-by-default is inverted, since a weak null proves nothing). No task surface, no script, no `capabilities.d/` entry ships; the lab "build" requires promotion to a full spec and is gated on the strong-foil resourcing decision.

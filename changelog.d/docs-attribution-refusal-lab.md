### Added

**ROADMAP "no-verdict-about-the-person" anti-goals + `specs/21-attribution-refusal-lab.md` (docs only).** Names the second anti-goal cluster the list was silent on — **no open-set author de-anonymization**, **no demographic / author profiling**, **no sock-puppet / cross-document identity-linkage verdict** — as the sibling of the existing *no-verdict-about-the-text* refusals, with the reasoning (base-rate / open-set error model; ESL-as-confound-not-label; linkage as the front end of unmasking). Adds the constructive carve-out as `specs/21` — a **dispatchable (build-gated) spec** for a **refusal-curve laboratory** on the existing `validation` surface that studies these three only to demonstrate where the signal is bullshit and score an untooled LLM's confident attribution against known ground truth. Strength-of-evidence only (LR / posterior odds / proximity), never a name or label. The contract specifies a harness entrypoint, a lab-manifest schema, aggregate-only output, the claim license, and three **go/no-go gates** — consent (no live/unconsented corpus), redaction (the testable E3 non-leak rule: raw person-identifying outputs are retained as private evidence under a bounded access/audit + deletion policy — `--private-store` + `--retention-days` + `--purge` — and never published; a scan fails the build on any leak), and **foil-strength** (a weak foil's null is marked `INCONCLUSIVE_WEAK_FOIL`, not a debunk) — plus a Test contract. The code build (and its `capabilities.d/` entry) is a separate PR gated on the strong-foil resourcing decision; this doc ships no script or surface.

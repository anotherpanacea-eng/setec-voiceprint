### Fixed

**The `tools/` doc/capability gates no longer crash printing a status glyph on a non-UTF-8 console.** `check_docs_freshness.py`, `check_capabilities_drift.py`, and `gen_calibration_readiness.py` (all CI-invoked) plus `seed_capabilities.py` print Unicode glyphs (`✔`, `≥`, `→`, `⇒`) to stdout/stderr. Under a `cp1252` default console — Windows with `PYTHONUTF8` unset — `print()` raised `UnicodeEncodeError` *after* the check had already run, turning a pass into a nonzero exit + traceback. CI is Linux/UTF-8 so it never fired there; it only bit a maintainer running the gate locally on Windows ("run the real CI command first"). A shared `tools/_console.enable_utf8_stdio()` now reconfigures stdout/stderr to UTF-8 at the top of each tool's `main()` — preserving the glyphs (local↔CI parity), a no-op where the streams are already UTF-8, and guarded (tuple `(AttributeError, ValueError)`) so it can never itself raise under a non-reconfigurable test stream. Pinned by `test_tools_console_utf8.py`, a cp1252-backed `TextIOWrapper` regression that reproduces the crash class on any platform. No capability, schema, envelope, or `plugin.json` change.

### Changed

**`narrative_decision_audit` (StoryScope) now lists `setec-voicewright` as a consumer alongside `apodictic`.** The capability fragment's `consumers` field gains `setec-voicewright` so the voicewright consumer's `scripts/sync_setec.py` projection + offline drift gate can vendor this surface for its spec-17 M3 work-level narrative diagnostic (a read-only check over an assembled long-form draft — never a selection/training target). This is a **consumer-membership change only**: the surface, its judge, its schema (1.0), its claim-license, and its `min_setec_version` floor (`1.107.0`) are all unchanged — adding a consumer does not alter the surface or its floor. The downstream consumer picks this up on the next tagged release (its sync resolves the latest release tag), so it requires a setec-voiceprint release **above the currently-vendored floor** (the v3 plan targets v1.117.0; the absolute version is producer-side). `plugin.json` is not bumped (versions bump at release). The contract golden (`_golden_capabilities.json`) is updated to match.

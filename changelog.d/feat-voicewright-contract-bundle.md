### Added

**setec-voicewright joins the normalized-entrypoint contract (second consumer) — four surfaces promoted to the consumer bundle (`voice_fingerprint`, `mimicry_cosplay_audit`, `general_imposters`, `binoculars_audit`).** Each fragment now carries the full R1 bundle (`min_setec_version: "1.115.0"`, `json_delivery`, structured `inputs[]`) plus `consumers: [setec-voicewright]`; `voice_distance` and `idiolect_detector` (already promoted for apodictic) add `setec-voicewright` to their `consumers:` lists — idiolect because its JSON is `mimicry_cosplay_audit`'s required cross-check input. The two previously seeded-stub fragments (`mimicry_cosplay_audit`, `general_imposters`) are fully curated, statuses glossary-anchored (`heuristic` — cosplay thresholds are calibration-pending — and `literature_anchored` per Koppel et al. 2014 / Kestemont et al. 2016 respectively). `general_imposters` is the third `json_delivery: file` surface (its `--json-out` is privacy-gated under `ai-prose-baselines-private/`, the `pov_voice_profile` pattern). `binoculars_audit` gains a `--json` flag (envelope on stdout, R2 consumer delivery; default evidence-pack writes are skipped under `--json`, explicit `--out`/`--out-md` still honored — its `results.perplexity_ratio` is the holdout scalar consumers read). Contract goldens grow 9 → 13 (`gen_contract_fixtures.py` builders for all four; existing nine goldens byte-unchanged), and the `--consumer setec-voicewright` query returns exactly the six-surface fitness set.

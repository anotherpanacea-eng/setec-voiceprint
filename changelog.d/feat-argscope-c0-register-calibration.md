### Added

**`argument_decision_audit` (ArgScope) gains register-baseline plumbing — `--register` / `--baseline-dir` (calibration ladder C0).** The surface can now read a register-matched row from the new `baselines/argument_register_baselines.yaml` (resolved via `--baseline-dir`, else `$SETEC_BASELINES_DIR`, else the shipped file; new helper `argument_register_baselines.py`, lazy PyYAML so the surface stays stdlib-only by default). Each matched signal's `contributions[]` now carry `register_human_mean` (+ `register_ai_mean` at `calibrated`) **alongside** the paper anchors, the per-signal `calibration_status` graduates off the default `literature_anchored` per the row's status, and `results.target.register` records the applied genre, source, and per-signal tiers. The seed ships **one** genre — `op-ed`, re-stating the paper's public-debate-forum human means at `literature_anchored` (op-ed is the paper's own register) — so the band stays `uncalibrated` and **no verdict changes**; C0 ships the schema + plumbing so the corpus-built `empirically_oriented` / `calibrated` rows (C1/C3) drop straight in. An unknown `--register` genre falls back to the paper anchors with a `register_warnings` note; a baseline that violates the honesty discipline (an above-`heuristic` status with no provenance, an unknown status, an out-of-range proportion, or `calibrated` without measured discrimination) fails loud as bad input. Envelope change is additive (schema 1.0); `argument_decision_audit.py` → 0.2.0; capability `inputs[]` + the contract golden updated. Calibration spec: `.argscope-spec/argscope-calibration-SPEC.md` §6 C0.

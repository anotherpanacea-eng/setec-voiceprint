### Added

**`capabilities.py emit --json` — a single machine-readable capabilities query for consumers (R1 of `references/setec-normalized-entrypoint-spec.md`).** Emits the whole manifest as one JSON document with a top-level `setec_version` (read from `plugin.json`) and `manifest_schema_version`, so a consumer (apodictic) can discover the version floor and the per-surface contract in one call instead of scraping individual YAMLs. Each of the nine apodictic-consumer subprocess surfaces — `variance_audit`, `voice_distance`, `voice_profile`, `pov_voice_profile`, `idiolect_detector`, `repetition_audit`, `punctuation_cadence_audit`, `manuscript_audit`, `narrative_decision_audit` — now carries `min_setec_version` (the version floor for that surface's JSON contract), `json_delivery` (how the envelope reaches the caller — stdout vs. file), a structured `inputs[]` list (each required/optional argument, typed, replacing prose-only usage notes), and a projected per-surface `calibration_status`. Five under-tagged shims (`manuscript_audit`, `repetition_audit`, `voice_profile`, `pov_voice_profile`, `punctuation_cadence_audit`) were corrected to `consumers:[apodictic]` + `handoff:stable` and curated from `status:todo` placeholders into honest stable contracts (status/family/purpose/use_when sourced from their scripts and same-family siblings), with a new drift check forbidding the `handoff:stable` + `status:todo` contradiction. The drift linter requires the full field bundle (`min_setec_version` / `json_delivery` / `inputs[]`) whenever `min_setec_version` is present, so a surface can't advertise a floor without the rest of the contract.

**`references/contract_fixtures/` — golden envelope fixtures + `fake_setec.py` + a `fixture_matches_build_output` drift gate (R5 of `references/setec-normalized-entrypoint-spec.md`).** One canonical `schema_version` 1.0 golden envelope per apodictic consumer surface (nine goldens) generated deterministically by `scripts/gen_contract_fixtures.py` through the real `build_output` path and the real `claim_license` registry, with volatile fields (timestamps, absolute paths) normalized so the goldens are reproducible. Ships a stdlib-only `fake_setec.py` reference fake for the consumer to vendor (so apodictic can test its adapter without invoking SETEC), and a `fixture_matches_build_output` drift check wired into `tools/check_capabilities_drift.py` so any change to the envelope shape fails CI until the goldens are regenerated (`gen_contract_fixtures.py --check`). A `.gitignore` negation re-includes the nine sentinelized goldens under the `*_voice_profile.json` privacy ratchet, and a directory guard asserts the fixtures dir holds only those nine known goldens so a stray real voice-clone artifact can never slip past the ratchet.

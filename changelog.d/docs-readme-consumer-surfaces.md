### Changed

**README now documents the decision-audit consumer surfaces.** The "five task surfaces" taxonomy describes the headline laptop *workflows*; the `narrative_decision_audit` (StoryScope) and `argument_decision_audit` (ArgScope) surfaces are a different category — `consumers: [apodictic]` JSON handoffs that score structural narrative/argument decisions against a paper's human/LLM means. Adds a short "Decision-audit surfaces (consumer handoff)" subsection so the README reflects them without conflating them with the five user workflows. Docs only.

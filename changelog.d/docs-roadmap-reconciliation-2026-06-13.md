### Changed

**ROADMAP status-reconciliation refreshed for the 1.110 → 1.116 window.** The authoritative reconciliation was dated 2026-06-06 / "plugin is at 1.109.x" and predated the normalized-entrypoint contract, the ArgScope decision-audit family, and the v1.116.0 release; its "What's left" still described the EPUB/manuscript acquirers as unmerged PRs. Adds a dated `## Status reconciliation (2026-06-13)` section (supplementing, not rewriting, the prior pass) recording what shipped since: the R1–R5 consumer contract (`capabilities.py emit --json`, `setec_run` dispatcher, R3/R4 gates, R5 fixtures) with apodictic + setec-voicewright as consumers; ArgScope Inc A1 (`argument_decision_audit` + `argmove_profile`); the now-merged `acquire_epub`/`acquire_manuscript` tooling; the `judge_backends`/`r1_bundle` dedups and CI consistency-gates; and the 1.110 → 1.116.0 releases. Docs only. (Part of #196.)

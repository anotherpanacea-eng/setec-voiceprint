### Fixed

- Preserve known `consent_status`, `era`, and `acquired_via` metadata when
  `acquire_manuscript` or `acquire_epub` emits identity-baseline entries,
  without changing deliberately unroled validation manifests.
